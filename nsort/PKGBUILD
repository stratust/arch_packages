# Maintainer: Anders Mårtensson <anders@nemrod.se>
pkgname=nsort
pkgver=3.4.45
pkgrel=1
pkgdesc="Nsort is a sort/merge program that can quickly sort large amounts of data, using large numbers of processors and disks in parallel."
arch=('x86_64')
url="http://www.ordinal.com"
license=('unknown')
makedepends=('rpmextract')
options=('emptydirs')
source=("http://www.ordinal.com/try.cgi/nsort-x86_64-3.4.45.rpm")
md5sums=('8800fa552b53dd96fba3d4a6053ee839')

build() {
  cd "$pkgdir"
  rpmextract.sh ../nsort-x86_64-3.4.45.rpm
}
